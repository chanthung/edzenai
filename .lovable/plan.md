

## Plan: Replace Parent Link slide image

**What changes:**
1. Copy the uploaded image (`user-uploads://image-166.png`) to `src/assets/slide-parent-link.png`, overwriting the existing file
2. No code changes needed — the carousel already imports from that path

**Files to change:**
- `src/assets/slide-parent-link.png` — replace with new screenshot

